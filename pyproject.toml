[project]
name = "pyp6xer-mcp"
version = "0.2.0"
description = "MCP server for parsing and analysing Primavera P6 XER schedule files"
requires-python = ">=3.13"
dependencies = [
    "fastmcp==3.2.4",
    "httpx==0.28.1",
    "mcp==1.27.0",
    "pydantic==2.12.5",
    "xerparser==0.13.9",
]

[project.scripts]
pyp6xer-mcp = "server:main"
